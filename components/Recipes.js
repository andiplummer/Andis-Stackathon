import React, { Component } from 'react'
import {Text, 
  View, 
  StyleSheet, 
  ScrollView, 
  Modal, 
  TextInput, 
  Image,
  TouchableHighlight, 
  SafeAreaView, 
  TouchableOpacity} 
  from 'react-native'

  export class Recipes extends Component {
    render() {
      return (
        <ScrollView>
          <Text>Recipes</Text>
        </ScrollView>
      )
    }
  }